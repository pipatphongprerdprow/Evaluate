<template>
    <button @click="login">Login with MSU</button>
  </template>
  
  <script setup>
  /*
  import { inject } from 'vue'
  
  // Inject the vueAuth plugin
  const vueAuth = inject('vueAuth')
  
  const login = async () => {
    if (process.client && vueAuth) {
      try {
        // Perform authentication
        await vueAuth.authenticate('msu')
        console.log('Logged in successfully!')
      } catch (error) {
        console.error('Login failed:', error)
      }
    } else {
      console.error('vueAuth is not available')
    }
  }
    */
  </script>
  